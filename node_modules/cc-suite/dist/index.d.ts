import { BinlookupOptions } from './bin_lookup';
declare class CCSuite {
    private _validator;
    private _generator;
    private _bin;
    private static instance;
    static getInstance(): CCSuite;
    constructor();
    validateCard(cardNumber: string): boolean;
    generateRandomCard(bin?: string, length?: number): string;
    getCardType(cardNumber: string): string | undefined;
    setBinConfig(config: string | BinlookupOptions): Promise<void>;
    getBinInfo(bin: string): Promise<any>;
}
declare const _default: CCSuite;
export default _default;
